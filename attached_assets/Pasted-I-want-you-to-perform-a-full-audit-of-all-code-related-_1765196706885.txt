I want you to perform a full audit of all code related to page transitions and the desert dust video overlay in this project.

Important:

Do not modify any files.

Do not auto-fix, refactor, or format anything.

Your job is to inspect, trace, and report, not to change code.

Context:

This is a Next.js + TypeScript + Tailwind project (Desert Rose Gin site).

I have a transition video with black background and desert dust stored as:

public/video/desert-wind-transition.webm

public/video/desert-wind-transition.mp4

I want this video to be used as a soft desert wind overlay between pages, not as a standalone full-screen page.

The goal is:

While the current page is visible, dust comes from the bottom and gradually covers the screen.

Under that, the next page is loaded.

Dust fades out and reveals the new page.

I previously asked for a DesertWindTransition overlay and an AppShell (or similar) to handle route changes, but right now I see no transition at all.

Your task:

Find all relevant components, hooks, and logic

Search the entire codebase for anything related to transitions or the desert wind video, including (but not limited to):

DesertWindTransition

AppShell

Any component/file with “transition” in its name

Any usage of desert-wind-transition in strings (video paths)

AnimatePresence / motion from framer-motion

usePathname, useRouter, or route-change logic in app/layout.tsx, _app.tsx, or other layout/shell components

List all files and components you find that are involved in page transitions or the dust video.

Verify video asset usage

Confirm whether desert-wind-transition.webm and .mp4 actually exist in public/video/ and how they are referenced in code.

Check that any <video> elements that should use this clip are pointing to the correct path /video/desert-wind-transition.webm and /video/desert-wind-transition.mp4.

Note any mismatches (wrong filenames, wrong folder, wrong URL, typos).

Audit overlay component logic

Locate the component (if it exists) that is supposed to render the dust overlay (for example DesertWindTransition).

Inspect:

Props (e.g. active: boolean)

How it uses framer-motion (AnimatePresence, motion.div)

How the <video> is configured

Check specifically:

Is the overlay container fixed inset-0 with a very high z-index (for example z-[9999])?

Does it have any background color (like bg-black) that would block the page?

Does the <video> have mix-blend-mode: screen (via Tailwind mix-blend-screen or custom CSS), or is it just a normal video?

Is muted, playsInline, and autoPlay or manual play() handling implemented correctly?

Note any issues that could cause the overlay to either not render, render behind content, or render as a full black screen instead of blended dust.

Audit route-change trigger logic

Find where route changes are detected:

usePathname from next/navigation, or

useRouter / router.events if using the Pages Router.

Identify the state that controls whether the transition is active (e.g. isTransitioning).

Check:

Is the transition state actually toggled on route changes?

Is there a hasMountedOnce flag that might be incorrectly preventing transitions?

Is the timeout/duration (setTimeout) in sync with the video length?

Is AppShell (or similar) actually wrapping the application in app/layout.tsx or _app.tsx?

List any reasons why the transition logic might never fire (e.g. AppShell not used, wrong key for AnimatePresence, wrong dependency array in useEffect, etc.).

Audit AppShell / layout integration

Inspect app/layout.tsx (or _app.tsx if Pages Router).

Check:

Is there a top-level wrapper like AppShell that renders {children} and the DesertWindTransition?

Is AppShell correctly imported and actually used in the layout?

Is AnimatePresence wrapping the routed content in a way that will trigger enter/exit animations on pathname change?

Note any structural issues (e.g. overlay rendered inside only one page instead of globally, or multiple overlapping shells).

CSS / Tailwind and blending audit

Search for mix-blend in the codebase.

Confirm whether Tailwind’s mix-blend-screen utility is available, or if a custom CSS class is defined.

Check global or layout CSS for anything that might break the effect:

Forceful black backgrounds on top-level containers.

Global overflow: hidden or z-index layers that might hide the overlay.

Note any conflicts that could cause the overlay not to be visible or the black background to stay visible.

Final report (no code changes)
Without changing any files, produce a clear, structured report with at least these sections:

A. Files and components involved

List each relevant file with a brief description (e.g. components/ui/desert-wind-transition.tsx — overlay component, etc.).

B. Video assets and paths

Confirm file existence and usage, list any mismatches or path issues.

C. Trigger logic (route changes)

Explain how the app currently detects route changes and how it is supposed to trigger the transition.

Point out any reasons why this might not be working.

D. Overlay behavior & blending

Explain exactly how the overlay is currently rendered, including blend mode, background, z-index, and layout.

Highlight any problems, such as missing mix-blend-screen, background colors, or wrong positioning.

E. Likely root causes of “no visible transition”

List the most probable causes, in order of severity/impact, that explain why I currently do not see the dust transition at all.

For each cause, mention where it is in the code (file + line range or component name).

F. Suggested fixes (high-level, no edits)

Write brief recommendations on how to fix each issue, but do not apply them.

Keep this at the “what to change” / “where to adjust” level, not actual edits.

Again:
Do not modify or auto-fix any code.