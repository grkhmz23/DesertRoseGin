You are working inside my existing “Desert Rose Gin” project in this Replit.

GOAL  
Add a sandstorm page-switch transition to the current site using the animation logic from the standalone HTML/JS demo I will provide below, WITHOUT replacing or breaking my existing layout, components, or routing.

Very important:  
- DO NOT replace index.html or the main React/JS entry with the demo HTML page.  
- DO NOT remove or radically change the existing design, typography, layout, or sections of the Desert Rose Gin site.  
- ONLY extract and integrate the sandstorm animation engine (canvas + JS logic) as a reusable overlay.

PROJECT ANALYSIS  
1. Inspect the codebase and identify:  
   - Whether this is a plain HTML/CSS/JS site, React, Vite, Next.js, or another framework.  
   - Where the main layout / root component is rendered (for example App.jsx, Layout.tsx, main.jsx, etc.).  
   - How navigation/section switching currently works (router, scroll, buttons, custom state, etc.).

2. Based on what you detect, choose the appropriate integration strategy:
   - If it is React or similar: implement the sandstorm as a reusable component that can be triggered before navigation or section change.
   - If it is a static HTML site: add the canvas + script in a way that wraps existing page sections and triggers the transition before swapping visible sections.

SANDSTORM CODE TO INTEGRATE  
Below is a standalone HTML file that contains body, pages, and a complete sandstorm engine.  
You MUST NOT copy its dummy pages (#page-dunes, #page-oasis) into my project.  
You MUST ONLY reuse the canvas element and the JavaScript logic related to sandstorm animation and transition.

[PASTE THE GEMINI HTML/JS CODE HERE]

REQUIRED IMPLEMENTATION

A) Canvas Overlay  
1. Add a single <canvas id="sandstormCanvas"> overlay that sits on top of the entire app:  
   - position: fixed; top: 0; left: 0; width: 100vw; height: 100vh;  
   - very high z-index (for example 9999);  
   - pointer-events: none when idle, pointer-events: all during the storm.  
2. Make sure this canvas is rendered once, at the root layout level (for example in App.jsx, Layout.jsx, or main HTML file), so it covers all content but only shows animation during transitions.

B) Sandstorm Engine Refactor  
Take the JavaScript from the Gemini code that controls:  
- SandParticle and DustCloud classes  
- particles and clouds arrays  
- initStorm, animate, updateStormPhase, triggerTransition functions  
- stormIntensity, isStorming, phase state machine  
and refactor it so it works inside this project’s stack:

IF IT’S REACT (OR A SIMILAR COMPONENT MODEL):  
1. Create a new component, for example:  
   - src/components/SandstormTransition.jsx (or .tsx)  

2. Inside this component:  
   - Render the <canvas id="sandstormCanvas"> element.  
   - Use useRef to get the canvas DOM node and initialize ctx, width, height.  
   - Use useEffect to set up:  
     - resize handler  
     - animation loop with requestAnimationFrame  
   - Implement the SandParticle and DustCloud classes directly inside this component file (or a separate module if you prefer).  
   - Preserve the same visual behavior: vertical “wave” of dust that rises, covers the screen, then fades.

3. Expose an imperative API to trigger the storm from outside, using forwardRef + useImperativeHandle:  
   - public method: startStorm(onMidpoint: () => void)  
   - When startStorm is called:  
     - set phase to “build”  
     - start storm animation  
     - when stormIntensity reaches the “peak” phase (when screen is visually covered), call the onMidpoint callback so the caller can change the page/section while the screen is hidden  
     - then automatically enter “fade” phase and clear the canvas when done.

4. Make sure global variables from the original script are handled correctly (either use component-level refs/state or module-level variables, but avoid polluting window).

IF IT’S PURE HTML/JS:  
1. Keep <canvas id="sandstormCanvas"> in the main HTML template.  
2. Move all sandstorm logic into a separate JS file, for example /js/sandstorm.js.  
3. Expose a global function window.startSandstorm(nextCallback) that:  
   - runs the build/peak/fade sequence  
   - calls nextCallback at the peak, just like above.  
4. Do NOT keep the demo’s page-dunes/page-oasis elements. Instead, integrate with the site’s real sections, for example:  
   - hide current section and show next section inside the callback.

C) Integration with Navigation / Page Switch

1. Find how the current site switches pages/sections. Examples:  
   - React Router: router.push('/new-route')  
   - Next.js: useRouter().push('/new-route')  
   - Buttons that switch visible sections by setting local state  
   - Anchors/links that are handled by JavaScript.

2. Introduce a helper function/wrapper for navigation:  
   - For React: create a custom hook or utility, for example useSandstormNavigation, that accepts the SandstormTransition ref and wraps navigation calls.  
   - For plain JS: wrap existing navigation logic in a function that calls startSandstorm, then changes the view inside the callback.

Concrete behavior:  
- When the user triggers navigation (clicks a button, menu item, etc.):  
  - Instead of immediately changing the route or section, call startStorm(() => { performNavigation(); }).  
  - This ensures the storm builds up, covers the screen, THEN the new page/section becomes visible, then the storm fades out.

3. Only apply this transition to major page/section switches (for example, top-level navigation links), not to every small UI interaction, to avoid it feeling heavy.

D) Visual & Performance Constraints

1. Keep the existing Desert Rose Gin visual identity.  
   - Do NOT change typography, base layout, or colors of the main site.  
   - You can keep the sand colors used in the gradient and particles, but do not override page background permanently.  
2. Ensure the animation performs reasonably on desktop and mobile:  
   - If necessary, reduce MAX_PARTICLES on smaller screens.  
   - Do not run the animation continuously when idle. It should only animate during transitions.

E) Clean up

1. Remove any mock/demo content from the Gemini HTML (page-dunes, page-oasis, demo text, demo buttons).  
2. Keep the sandstorm code DRY: no duplicated functions or duplicated canvas elements.  
3. Add short comments in the code explaining how to trigger the storm and where to plug in navigation.

FINAL DELIVERABLE

- A working project where:
  - The site looks the same as before.  
  - There is a reusable sandstorm overlay component or module.  
  - I can trigger a desert-style sandstorm transition before a page or section change, with the new content appearing while the screen is covered, and the storm fading out smoothly.

After you finish, summarise:
- Which files you created or modified.  
- How to call the transition from code (with one or two concrete examples).
