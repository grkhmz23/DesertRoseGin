I have two hero animation files that I want to use for the Desert Rose Gin website:

hero.webm (VP9, 2560×1396, 24 fps, about 11 MB)

hero.mp4 (H.264, 2560×1396, 24 fps, about 22 MB)

I want you to:

Assume these files will be placed in the public assets folder under /video/hero.webm and /video/hero.mp4.

If this project uses a public folder (like Next.js, Vite, CRA), put them in public/video/.

If the structure is different, adapt paths accordingly but keep them under some /video/ route.

Implement a hero section that:

Uses a <video> element as the background hero animation.

Autoplays immediately when the page loads.

Is muted and plays inline (no fullscreen on mobile).

Has no controls and shows no play button.

Plays once and stops on the last frame (do not loop).

Is not scroll-driven. The animation runs on its own, and after it finishes the user can scroll normally.

Use both video formats and let the browser choose:

WebM first, MP4 fallback. Example HTML structure:

<section class="drg-hero">
  <video
    class="drg-hero-video"
    autoplay
    muted
    playsinline
    preload="auto"
  >
    <source src="/video/hero.webm" type="video/webm" />
    <source src="/video/hero.mp4" type="video/mp4" />
  </video>

  <div class="drg-hero-overlay">
    <!-- Place any logo, tagline, button, or hero content here -->
    <!-- Example:
    <h1>Desert Rose Gin</h1>
    <p>Handcrafted small batch gin.</p>
    -->
  </div>
</section>


Make sure the final code in the project uses this logic, even if the syntax is adapted for React, Next.js, or another framework.

Add CSS so the video behaves like a full-bleed hero background:

The hero should take full viewport height (100vh).

The video should cover the entire hero area, centered, with no black bars, using object-fit: cover.

The overlay content should be centered and appear above the video.

Example CSS (adapt and integrate into the existing styling system):

.drg-hero {
  position: relative;
  width: 100%;
  height: 100vh;
  overflow: hidden;
}

.drg-hero-video {
  position: absolute;
  top: 50%;
  left: 50%;
  min-width: 100%;
  min-height: 100%;
  width: auto;
  height: auto;
  transform: translate(-50%, -50%);
  object-fit: cover;
}

.drg-hero-overlay {
  position: relative;
  z-index: 1;
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  /* You can add padding, color, or gradient overlay if needed */
}


Integrate this hero section into the actual homepage entry point:

If this is a React/Next.js project, create a Hero or HeroSection component and use it on the main page (pages/index.tsx, app/page.tsx, or equivalent).

If this is a plain HTML project, update the main page template to include this <section> at the top of the body as the first visible block.

Make sure:

There is no default video control bar visible.

The video does not require user interaction to start.

It works on Chrome, Edge, Firefox (via WebM) and on Safari / iOS (via MP4).

The behavior is consistent on desktop and mobile (autoplay, muted, playsinline).

Please show me the exact code changes (HTML/JSX/TSX + CSS) you make and confirm the final paths you expect for hero.webm and hero.mp4, so I can upload the files correctly.